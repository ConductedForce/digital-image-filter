"""
Procedure:

Read images.
Convert to usable format. (pixel format)
hold images in array

loop through rgb of images
store rgb
get median of r list,g list,b list
store in new image


Repeat until all pixels have been read.
Save image.
Done.
"""

#test code
#im = Image.open('sampleImages\one.png')
#px = im.load()
#image_list[1].show()
#print(len(image_list));

#may not actually be neccesary
#if (width > height):
 #   temp = height
  #  height = width
   # width = temp